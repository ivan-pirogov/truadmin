# Server Configuration
SERVER_PORT=8080
GIN_MODE=release

# Database Configuration (SQLite for storing connections, users, scripts)
DB_PATH=./data/truadmin.db

# JWT Secret
JWT_SECRET=your-secret-key-change-in-production

# PostgreSQL Connection (optional - user can create connections via UI)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=truadmin
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123

# Snowflake Connection (optional - user can create connections via UI)
SNOWFLAKE_ACCOUNT=
SNOWFLAKE_USER=
SNOWFLAKE_PASSWORD=
SNOWFLAKE_DATABASE=
SNOWFLAKE_WAREHOUSE=
SNOWFLAKE_SCHEMA=public
